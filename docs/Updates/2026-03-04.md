<!-- 编码：UTF-8 -->

# 2026-03-04 更新日志

## 【面向用户】

### v0.2.003 前端体验与稳定性重构收口
1. 登录、注册、找回密码、设置页与后台页面交互更稳定，页面加载更一致。
2. 工作流侧边栏支持右键重命名与删除，操作后列表会自动刷新。
3. 后台导航与移动端侧边栏行为一致，切换页面时体验更顺滑。
4. 修复了一批已知问题并优化了错误提示与边界处理。

---

## 【面向开发者】

### v0.2.003 Frontend Full Refactor Close-out
1. 完成 `app -> features -> hooks/services -> types` 分层收口，页面层保持薄入口。
2. 全仓行数治理落地：`src/** <= 300`，`app/**/page.tsx <= 220`，新增 `lint:pages` 门禁。
3. `middleware` 按 Next 16 规范迁移至 `proxy`。
4. Sidebar（用户侧/管理侧）完成纯函数 + hook 解耦；用户侧菜单接入真实重命名/删除能力。
5. 测试文件拆分完成并新增导航属性测试；`pnpm lint/test/lint:lines/build` 全通过。
